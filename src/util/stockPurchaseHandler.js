export function applyStockListChanges(
  dbRef,
  searchSymbol,
  stockList,
  shares,
  amtInv
) {
  //Changes the entry using the stock symbol
}

export function retIndividualStockDetail(dbRef, searchSymbol, stockList) {
  //Returns info about individual stock
  const retDetails = {
    sharesOwned: 0,
    investedAmount: 0,
  };

  return retDetails;
}
